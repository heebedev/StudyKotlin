


// 함수 내용 간단하게 선언하는 방법 (함수가 간단할 때만)
fun 함수명(변수명:타입, ...) = 함수내용

// 가변인자를 갖는 함수

fun 함수명(vararg 변수명:타입) : 반환형 {
    variable vararg

    return 반환값
}


1. Unit (반환형이 없을 때)
- 자바 : -> void

2. Any/Any?
- Any : non-null 타입의 조상
- Any상? : nullable 타입의 조상

3. Nothing
fun 함수명(변수명: 타입, ...) : Nothing {

}
- 이 함수를 호풀하면 뭔가 잘못됐다
- 명시적으로 알려준다.